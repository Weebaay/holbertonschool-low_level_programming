#include "main.h"

/**
* _abs - computes the absolute valeur of a integer.
*@c: return
*
*description : blabla
*Return: 0 always
 */

int _abs(int c)
{
	if (c < 0)
	{
		return (-c);
	}
	else
	{
		return (c);
	}
	return (0);
}
