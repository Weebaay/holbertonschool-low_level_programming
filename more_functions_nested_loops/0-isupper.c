#include "main.h"

/**
* _isupper - cheks for uppercase character.
*@c: cat caractere
*
*Return: 1 if c is uppercase or 0
*/


int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
